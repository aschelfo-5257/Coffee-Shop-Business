module coffee.shop.business {
    // Requires standard JDK desktop/logging tools if needed
    requires java.logging;

    // Only add exports/opens once those specific package folders physically exist
}
